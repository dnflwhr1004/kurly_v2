<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wooyeon.web.mappers.MemberMapper">

	
   <select id="existId" resultType="int" parameterType="String" >
    SELECT COUNT(*)
    FROM users
    WHERE userid LIKE #{userId}
   </select>
   
   <select id="selectNIP" resultType="com.wooyeon.web.domains.Member" parameterType="com.wooyeon.web.domains.Member">
   		select * 
   		from users
   		where userid like #{userId} and passwd like #{passwd}
   </select>
   
   <insert id="insert" parameterType="com.wooyeon.web.domains.Member">
       insert into users (userid, username, gender,  passwd, email, mobile, birthday, addr) values (
           #{userId}, #{userName}, #{gender}, #{passwd}, #{email},#{mobile},#{birthday}, #{addr}
       )
   </insert>
   
   
    <select id="count" resultType="int" >
    SELECT COUNT(*)
    FROM users
   </select>
   
   <select id="selectUserByIdPw" resultType="com.wooyeon.web.domains.Member" >
    SELECT *
    FROM users
    WHERE userid LIKE #{userId}
    AND passwd LIKE #{passwd}
   </select>
   
   <update id="update">
   	
   </update>
  
  <delete id="delete" parameterType="String">
  	DELETE 
  	FROM member
  	WHERE userid LIKE #{userId}
  </delete>
  
</mapper>